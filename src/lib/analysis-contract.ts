/* The app's entry point to the analyze-chart contract. The schemas live next to
   the Edge Function so both sides validate the exact same shape. */
export * from '../../supabase/functions/_shared/analysis-contract';
