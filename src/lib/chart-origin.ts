/* The app's entry point to a Chart's origin. The values live next to the Edge
   Function so both sides validate the exact same set. */
export * from '../../supabase/functions/_shared/chart-origin';
